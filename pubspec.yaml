name: treat_yoself
description: Crowd Sourced Shopping App

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  mysql1: ^0.17.1
  cupertino_icons: ^1.0.0
  json_annotation: ^3.0.1
  provider: ^4.3.2
  firebase_auth: ^0.20.0+1
  firebase_core: 
  firebase_analytics:
  cloud_firestore: ^0.16.0
  get: ^3.17.1
  get_storage: ^1.3.2
  flutter_sheet_localization: ^1.0.0
  simple_gravatar:
  

dev_dependencies:
  flutter_sheet_localization_generator: ^1.0.0
  flutter_test:
    sdk: flutter
  build_runner: ^1.0.0
  json_serializable: ^3.3.0

flutter:
  uses-material-design: true
